# گزارش مهندسی — بازطراحی «پنل هوشمند میلانو» (نسخه ۲)

**نویسنده/طراح:** Milad Yaghoobi · Meelano Studio Design
**تاریخ:** ۲۰۲۶-۰۹-۰۵ · **نسخه سامانه:** 2.0.0 (Quantum Bazaar)

این سند، آنالیز فنی نسخهٔ قبلی (فایل تک‌فایلی `gemini-code-*.html`)، عیب‌یابی دقیق،
و معماری سامانهٔ بازطراحی‌شده را مستند می‌کند.

---

## ۰) هشدارهای امنیتی فوری (بخوانید اول)

1. **نشت کلیدها:** کلیدهای OpenAI / Gemini / Groq / Stability / fal / Kling / Cloudflare و
   رمز دیتابیس به‌صورت متن آشکار در چت ارسال شده‌اند. هر کلیدی که در چت/ایمیل/گیت رد و بدل شود
   «لو رفته» تلقی می‌شود. **همه را از داشبورد هر سرویس باطل (Revoke) و دوباره صادر کنید.**
   این سامانه به‌گونه‌ای ساخته شده که کلیدها فقط در `config/settings.php` (رمزنگاری‌شده با
   AES-256-GCM و خارج از گیت) نگهداری شوند و هرگز به مرورگر برنگردند.

2. **دسترسی دیتابیس از بیرون بسته است (یافتهٔ راستی‌آزمایی):** پورت `3306` روی
   `<db-host>` از بیرون توسط فایروال cPanel بسته است (TCP باز می‌شود اما دست‌دادن
   MySQL فوراً قطع می‌گردد). یعنی اتصال از یک سرور خارجی ممکن نیست؛ برنامه باید **روی همان هاست**
   (`ainetmee.ir/trader`) اجرا شود و با `host = localhost` وصل شود. مقدار پیش‌فرض این سامانه
   `localhost` است.

3. **رمز پیش‌فرض مدیر:** تا وقتی رمز را عوض نکرده‌اید، ورود تنظیمات با `meelano-admin` ممکن است.
   پس از ورود، از تب «امنیت» آن را تغییر دهید.

---

## ۱) عیب‌یابی نسخهٔ قبلی (فایل HTML تک‌صفحه‌ای)

| # | مشکل | شدت | راه‌حل در نسخهٔ ۲ |
|---|------|------|-------------------|
| 1 | `form action="save_product.php"` اما فایل سرور وجود نداشت → ثبت عملاً کار نمی‌کرد | بحرانی | `api/product_save.php` + مخزن `ProductRepository` با ذخیره/فراخوانی واقعی |
| 2 | «هوش مصنوعی» یک آرایهٔ سخت‌کدشده و `alert` بود؛ هیچ API واقعی صدا زده نمی‌شد | بحرانی | `Ai\Client` با ۱۲ ارائه‌دهنده واقعی + مسیریابی و fallback |
| 3 | قیمت‌های ترب/دیجی‌کالا ساختگی (hardcoded) و بدون منبع/اعتبار | بالا | `Market\Aggregator` با برچسب منبع (`torob_api/digikala_api/ai_estimate`) + اعتبار |
| 4 | تولید تصویر فقط یک URL ثابت Unsplash بود | بالا | `api/image.php` با رندر واقعی و تبدیل خودکار به WebP در ابعاد دقیق |
| 5 | کلیدهای API باید در فرانت‌اند بود → نشت کامل | بحرانی | کلیدها فقط سمت سرور، رمزنگاری‌شده، و پنهان در UI |
| 6 | هیچ دیتابیس/جدول/نصب/سلامت‌سنجی وجود نداشت | بحرانی | `Schema` + `Installer` (SSE) + `api/db_test/db_status` |
| 7 | بدون احراز هویت/CSRF/محدودسازی نرخ روی عملیات حساس | بحرانی | `Security` (ورود مدیر، CSRF، rate-limit، سرصفحه‌های امن) |
| 8 | محاسبه قیمت بدون مدل مالی (مالیات/کمیسیون/سربه‌سر/ریسک) | بالا | `Pricing\Engine` قطعی و قابل حسابرسی |
| 9 | بدون ساختار اندروید و آیکون | متوسط | بستهٔ `android/` + آیکون تطبیقی سه‌بعدی |
| 10 | بدون فوتر برندینگ | ظاهری | فوتر «Meelano Studio Design / Milad Yaghoobi» |

---

## ۲) معماری نسخهٔ ۲

```
panelhoshmand/
├─ index.php / products.php / settings.php / login.php   ← صفحات
├─ api/            ← نقاط پایان JSON/SSE (db_test, db_install, ai_test,
│                    ai_autoroute, analyze, market, price_calc, image,
│                    ai_video, product_save, products, settings_save, health)
├─ includes/       ← هسته: Config, Db, Schema, Installer, Security, Logger,
│                    ProductRepository, layout
│  ├─ Ai/          ← Client, Router, Registry, Health, Transport/Curl/Mock
│  ├─ Pricing/     ← Engine
│  └─ Market/      ← Aggregator
├─ assets/         ← css/app.css، js/{core,panel,settings,products}.js، icon/
├─ android/        ← AndroidManifest، MainActivity، res (mipmap + adaptive icon)
├─ tools/          ← build_icon.py، import_secrets.php
├─ tests/run.php   ← ۳۹ تست که کد واقعی را اجرا می‌کنند
└─ config/         ← (gitignore) settings.php رمزنگاری‌شده + .app_key
```

### ۲٫۱) لایهٔ هوش مصنوعی — «مسیریابی هوشمند»
- `Registry` ماتریس توانمندی ۱۲ ارائه‌دهنده × ۱۳ وظیفه را نگه می‌دارد
  (امتیاز تخصص، کلاس سرعت/هزینه، قابلیت‌ها).
- `Router.rank()` امتیاز هر ارائه‌دهنده را از جمعِ: توانمندی + تخصص + سلامت
  اندازه‌گیری‌شده + سرعت + هزینه محاسبه می‌کند و `autoRoute()` بهترین را برای هر
  وظیفه انتخاب و زنجیره پشتیبان می‌سازد (دکمه «انتخاب هوشمند خودکار»).
- `Client` با زنجیره پشتیبان: اگر موتور اول شکست خورد، خودکار بعدی را امتحان می‌کند
  و همه فراخوانی‌ها را در جدول `ai_tasks` ثبت می‌کند (تأخیر/توکن/وضعیت).

### ۲٫۲) قیمت‌گذاری قطعی
`Pricing\Engine` ریاضیات را جدا از AI نگه می‌دارد تا قابل حسابرسی باشد:
`BE = C/(1−d)` · `P = min(C/(1−m−d), کف‌بازار×ضریب)` · `π = P − C − P·d`.
خروجی شامل سربه‌سر، حاشیه، ROI، پلکان عمده، جدول حساسیت، کشش تقاضا و ممیزی ریسک است.

### ۲٫۳) نصب دیتابیس با گزارش لحظه‌ای
`api/db_install.php` با **SSE** رویداد می‌فرستد؛ UI نوار درصد و نام جدول در حال ساخت را
زنده نشان می‌دهد. نصب ایدم‌پوتنت است و دسته‌بندی‌ها یک‌بار seed می‌شوند.

### ۲٫۴) چک خودکار هنگام ورود
`api/health.php` و `refreshSystemStatus()` در هر بار ورود، اتصال دیتابیس، کامل‌بودن جدول‌ها،
سلامت کلیدهای AI و وضعیت مسیریابی را بررسی و چیپ‌های بالای صفحه را رنگی می‌کنند.

---

## ۳) راستی‌آزمایی انجام‌شده (در همین مخزن)

- `php -l` روی همه فایل‌های PHP: بدون خطا.
- `tests/run.php`: **۳۹ تست، ۰ خطا** — شامل فرمول سربه‌سر، ایدم‌پوتنت‌بودن نصب، ذخیره/فراخوانی
  کالا، تشخیص کالای مشابه، مسیریابی (normalize→Groq، تصویر→Stability، ویدیو→Kling)،
  fallback خودکار، Health، تجمیع بازار، و رمزنگاری Config.
- E2E نقاط پایان با SQLite: نصب (۱۳ جدول SSE) → ذخیره کالا → فهرست → وضعیت: همگی موفق.

*یادداشت:* چون در این سندباکس خروج مستقیم اینترنت و MySQL بیرونی بسته است، لایهٔ HTTP با
`MockTransport` تست شده و اتصال MySQL واقعی فقط از داخل هاست شما قابل راستی‌آزمایی است.

---

## ۴) استقرار روی هاست شما (`ainetmee.ir/trader`)

1. کل محتوای مخزن را در مسیر `trader` آپلود کنید.
2. از `settings.php` وارد شوید (رمز پیش‌فرض `meelano-admin`) و تغییر رمز دهید.
3. تب «پایگاه‌داده»: host=`localhost`، نام/کاربر/رمز دیتابیس cPanel را وارد و «تست اتصال» و سپس
   «ساخت جداول» بزنید.
4. تب «هوش مصنوعی»: کلیدهای **نو** را وارد و «تست همه کلیدها» و سپس «انتخاب هوشمند خودکار» بزنید.
5. در صورت نیاز PHP ≥ 7.4 و افزونه‌های `curl, gd, pdo_mysql, mbstring, openssl` را فعال کنید.

---

## ۵) محدودیت‌های شناخته‌شده (شفافیت)

- نقاط پایان غیررسمی ترب/دیجی‌کالا ممکن است تغییر کنند؛ در خطا، سامانه به برآورد AI با
  برچسب «اعتبار محدود» سقوط کنترل‌شده می‌کند و هرگز عدد را به‌عنوان قطعی جا نمی‌زند.
- موتورهای ویدیو ناهمگام‌اند؛ شناسه پیگیری برمی‌گردد و رندر چند دقیقه طول می‌کشد.
- کشش تقاضا یک مدل خطی محافظه‌کارانه است، نه پیش‌بینی قطعی بازار.
